import React, { Component } from 'react';

export const Header = (props) => (
  <div id="header-bar">
    <img class="logo" />
    <ul class="user-dropdown">
      <li>Servio</li>
    </ul>
  </div>
)
