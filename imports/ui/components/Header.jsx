import React, { Component } from 'react';

export const Header = (props) => (
  <div id="header-bar">
    <img className="logo" />
    <ul className="user-dropdown">
      <li>Servio</li>
    </ul>
  </div>
)
